const {} = require('./helpers');

const partOne = input => {};

const partTwo = input => {};

const solve = (input, part) => (part === 1 ? partOne(input) : partTwo(input));

const expected = part => (part === 1 ? '' : '');

module.exports = { solve, expected };
